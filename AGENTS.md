# SpotNote Agent Runbook

SpotNote's stable source of truth on David's Mac is this repo:

```text
/Users/davidbeyer/Projects/SpotNote
```

Do not build or install SpotNote from disposable worktrees such as `.hermes/kanban`, `.paperclip-*`, `/tmp`, or ad hoc recovery directories. Those are allowed for experiments only; any accepted work must be copied or recreated here, git-tracked, tested, and committed before it replaces `/Applications/SpotNote.app`.

## Current custom contract

The local David build intentionally differs from upstream SpotNote:

- Catppuccin and Rosé Pine themes are present in `Sources/Spotlight/Theme.swift`.
- Linear handoff is routed through `Sources/Spotlight/ScratchpadHandoff.swift` to the local Hermes ingress endpoint.
- Hermes feedback uses `Sources/Spotlight/HermesToastView.swift` plus `Sources/Spotlight/Resources/HermesLogo.png`.
- The first/default note is the vault-backed Markdown inbox at `~/Documents/knowledge/Captures/spotnote-inbox.md`; JSON chat persistence is secondary app-local library state, not the launch buffer. Recognized section headings are **Title Case** -- `## Todo`, `## Tray` -- and the inbox loader normalizes any recognized heading (any case, any `#` level) to that canonical spelling. The loader does NOT inject or reorder sections; header-less notes stay header-less, and a fresh/missing inbox opens blank instead of inserting a default section.
- The old Vim statusline is removed entirely: no `vimBarHeight` constant and no `VimStatusLine.swift` source file should exist.
- The floating action menus ride a borderless child panel (`RaycastModalWindow.swift`) whose frame clamps to the anchor screen's `visibleFrame` bottom (minus the 40pt shadow margin): on a short bottom-pinned note the sheet slides UP over the note body instead of rendering below the display edge.
- Native Flash-style Vim jumps are restored without terminal embedding or statusline chrome: `VimFlash.swift`, `MultilineEditorFlash.swift`, and `MultilineEditorFlashRendering.swift` own the `S` whole-document backward jump, `f`/`F` same-line jumps, and `K` row/gutter labels. Normal/visual `s` is the hop-style word-hint jump (`VimWordHint.swift` + `MultilineEditorWordHint.swift`): every visible word start gets an instant label (hop.nvim TrieBacktrackFilling charset/order, nearest-first by Manhattan distance with x_bias 10), typed keys narrow labels (never a search query), the document dims while hints show, and label colors are the nvim pair (`#F75057` primary -- the `hop_red` role, rose-pine love pulled 0.6 toward true red; bare love `#eb6f92` is PINK and was a diagnosed nvim regression, do not reintroduce it -- / `#9ccfd8` foam alternate for touching runs). Both word-hint and Flash labels render as BARE colored letters, baseline-aligned in the anchor's character cell (`drawHintLabel`; word hints plain, Flash bold) -- nvim's foreground-only `hl_mode = "replace"` look; the opaque pink pill chips read as "gross background ovals" (David, 2026-08-10). The covered glyphs are hidden with a `.clear` TEMPORARY foreground attribute at refresh time (layout keeps every advance, so nothing reflows); never hide them by EDITING the string -- character replacement garbled the note text in this proportional editor (David, 2026-08-09). The hidden span is advance-MEASURED (`hintHiddenRange` in `MultilineEditorFlashRendering.swift`): the helper walks composed characters from the anchor accumulating advances until the label's ink fits (min one char, stops at a newline). On today's mono grid that equals label-length cells, but keep the measured walk -- a fixed label-length hide is exactly what broke under the earlier proportional editor ("the flash hints are all messed up", David 2026-08-10: wide labels collided with the next glyph, narrow ones left stray ink), and the helper is font-agnostic.
- The editor font is **Lilex Nerd Font Mono** (`LilexNFM-Regular`/`-Bold` bundled in Resources, system-mono fallback) at the Raycast Notes body scale via `SpotNoteFont.editor()` -- the SAME face David's nvim renders in (Ghostty `font-family`; IBM Plex Mono letterforms + ligatures). David ordered the mono grid back on 2026-08-10 ("switch to mono grid and use the same mono font as my nvim") after the proportional 2026-08-08 "exact same look" era made the hop/flash letter hints gappy; only the note BODY is mono -- all Raycast chrome stays Inter via `RaycastFont`. Bold/italic derive in-family via `NSFontManager` trait conversion (guarded by `boldFlashLabelsKeepTheMonoAdvance`).
- Task checkboxes/sign markers are retired from the live editor. Old Markdown `[ ]` / `[x]` storage markers may still parse for compatibility, but SpotNote should not render, reserve, or toggle checkbox gutter chrome. Task completion/status now flows through Linear motions.
- Markdown outline behavior belongs in `MarkdownOutline.swift` and `PlaceholderTextView`: Enter and Vim normal-mode `o`/`O` continue `- ` bullets with the current indentation, Tab indents a bullet by one two-space level, Shift-Tab outdents by one level, and pressing Enter on an empty bullet exits the list. Motions are split by prefix: the `,` prefix jumps to a section and inserts a fresh `- ` bullet at its end (creating it if absent, ignoring internal blank spacers): `,d` → `## Todo`, `,t` → `## Tray`. The `g` prefix is reserved for Linear handoff: `gd/gp/gs/gt/gl` send the bullet to David's Personal workspace at Done/Planned/Started/Triage/Later, and `gc` sends it to his Code workspace at Triage with the assignable `Develop` label. The `\` leader handles other sends and is reusable: `\t` appends the current line to spotnote-tray.md; `\c` appends the current bullet block(s) to the hermes-build State.md (`~/Documents/knowledge/Work/hermes-build/Notes/State.md`) as one flattened `- ` line per block, then clears the source (`N\c` files N separate `- ` lines, not one fused line); and `\f` (or `:fmt`) tidies blank-line spacing around section headers (one blank line above and below each header, none above the top header), preserving bullets and multiline bullets. (Legacy `gH/gD/gT/tt/gy`, `,h`, `,b`, and `\h` are retired.)
- Line numbers are REMOVED (2026-08-09, David: "I don't like the line numbers") -- no preference, no Settings row, no `:set number`. The editor reserves no gutter (`LineNumberRuler.thickness(showsLineFlashHints: false, ...) == 0`) and uses `EditorMetrics.textLeadingGap == 37` for the Raycast-style body inset (pixel-measured from the live Raycast Notes window; the text edge is the parity contract). `LineNumberRuler` survives ONLY as the zero-width flash-hint gutter: it opens to `signColumnWidth` while vim `K` line-flash labels show, then collapses.
- The editor text is the Raycast Notes body scale, pixel-measured from David's live Raycast Beta Notes window (2026-08-08): `EditorMetrics.fontSize == 20` with `EditorMetrics.lineHeight == 41` (their line box + paragraph spacing, applied uniformly), panel width 670, on a full-bleed 26pt-radius surface (circle-fit on the live Raycast window border arc). Themes with `Theme.backgroundTop` (raycast-dark: `#2A2C3B` → `#252634`) render Raycast's full-height vertical surface gradient; flat themes keep the near-opaque glass tint. ALL panels are borderless (`SpotlightWindowController.panelStyleMask`); the traffic lights are drawn by `RaycastTrafficLights` in SwiftUI (14pt dots, 23pt centers, Display-P3 close red `#EC6765` + two disabled greys) because Raycast Notes draws its own. ALL Raycast-chrome text (top-bar title, character counter, every modal row/header/chip/search field) renders in INTER via `RaycastFont` -- the live app ships `InterVariable.woff2` in its frontend bundle and none of its chrome is SF; the bundled `Inter-Regular/Inter-Medium.otf` register through `FontLoader`, and `.custom` falls back SILENTLY on a wrong PostScript name, so never rename them casually. The editor body is still the system font (switching it moves the measured line metrics -- David's call, not a drive-by). The top bar (60pt: hover-revealed lights, centered 16pt first-line title, trailing 44pt capsule -- solid `#25262E`, white-0.08 border -- with the exact @raycast/icons command raster, a custom fanned-cards note-switcher glyph, and a DRAWN 2pt round-cap plus -- `RaycastPlusShape`, 18pt visible; the bundled raster's stroke scales to a too-heavy 3pt at pill size, so the pill must not use it. Each pill icon sits in a uniform 30pt hover cell (`PillIconButton`) that shows a soft CIRCLE highlight under the pointer -- probed ~31-33pt diameter at a bare +8 RGB over the pill fill (white 0.04); a rounded rect at 0.08 read as the wrong shape in David's 2026-08-10 captures) and bottom bar (56pt: 16pt character counter only; theme switching lives in the actions modal, the old circled-T button is retired) follow Raycast's real resigned state (pixel-probed 2026-08-09): the pill and title STAY VISIBLE when the panel is not key -- the close light turns inactive grey like its siblings, pill icons dim to `#4C4E59` (`RaycastChromePalette.controlResigned`), and the title keeps its normal color. The traffic lights are the ONE hover-gated piece (David 2026-08-10): `RaycastTopBar.showsLights` fades the cluster in only while the pointer is over the window (or any modal is up -- a modal child window intercepts tracking events, so hover alone would drop them); the pill must NOT become hover-only. The bars are ALWAYS-present chrome: `EditorMetrics.topBarHeight`/`bottomBarHeight` are mirrored in BOTH `SpotlightRootView.extraChromeHeight` and the controller's `chromeAboveEditor`/`chromeBelowEditor`. Note browsing, actions, and the theme picker are Raycast-style floating modals (`RaycastNotesModal` over `FuzzyController` + `RaycastActionsModal` from the ⌘ pill button in `RaycastModals.swift`; `RaycastThemesModal.swift`): the editor stays UNDIMMED while a modal shows (Raycast probes (40,42,56) with the menu open, 2026-08-09 -- a transparent tap-catch in a `SpotlightRootView` `.overlay` only dismisses; do not reintroduce a dim), and the SHEET rides an overhanging borderless CHILD WINDOW (`RaycastModalWindow.swift`, presented via `.background(RaycastModalOverhang(...))`) so it extends past the panel's bottom edge exactly like the live app (probed: sheet top 100pt below window top, 383.5pt wide, the live menu runs ~427pt past the empty window's bottom). The child panel takes key for the search field; the controller's resign handler consults `RaycastModalOverhang.activeChildWindow` so the modal never dims/closes the HUD, the chrome passes `isKey || anyModalShown` to stay lit, and the child losing key dismisses the modal. Modals NEVER contribute panel height (guarded by `SpotlightRootToastTests.notesModalDoesNotTriggerPanelHeightCallbacks`). Menu geometry is pixel-probed (2026-08-09): the sheet is a TRANSLUCENT MATERIAL like the live app (behind-window `.popover` blur under a `#101219` tint at 0.45 -- the material/alpha pair chosen by swatch-lab MEASUREMENT against Raycast's ~21% backdrop bleed, the ink then corrected from David's 2026-08-10 SAME-DESK side-by-side: the earlier `#1D1D28` ink composited (+6,+5,+7) brighter than the live sheet ((37,38,49) vs (31,33,42)), and the delta belongs in the INK (divide by the 0.45 tint), not the alpha. A 0.90 tint matches composite but kills the bleed (t ~ 0.02) -- do not raise the tint to chase flat color; the "top-lit gradient" probed earlier was blur bleed-through, not paint; the child panel pins `.darkAqua` and owns the window-server drop shadow), bluish #4A4660 hairline border, radius 10; the search zone is 47pt in both modals (caret/placeholder center 47px at 2x -- round 10) over a 0.5pt white-0.08 hairline (a SINGLE pixel at 2x; our 1pt rule read twice as heavy); row highlights inset 9.5pt -- 728px wide inside the 766px sheet at 2x, content pads compensated so icon/title ink stays put -- and actions rows are 42pt TOUCHING, the live selected rect being one full 84px pitch (radius 6, selected #272733 -- live hovered rows probe (40,40,52) in the actions menu and (38,38,50) in browse, a ±2 noise band whose center serves both; the original #2B2B38 ran ~+4 hot and too blue), and the highlight FOLLOWS THE POINTER in both modals (hover lights a row without a click, staying on the last hovered row -- keyboard and pointer write the same selection); row icons are exact @raycast/icons rasters, frames tuned per raster padding so the VISIBLE glyph hits the live menu's ~15.5pt (probed 31px @2x on Duplicate/cards; the earlier 14pt frames rendered a visibly-small ~12.5pt) -- ratio-0.875 rasters get frame 17.5, `RaycastTextSearch` 19, and the plus stays SMALL at frame 18 (~11pt visible; the live New Note plus probes 21x23px, smaller than its siblings); cards glyph at scale 0.77; Find in Note uses `RaycastTextSearch` -- Raycast's lines+magnifier glyph is NOT in the public icon set, so its SVG sits in Resources beside the raster, composed from David's capture geometry) tinted in the PRIMARY text color #CFD6F1 -- the live rows read as one bright unit, do not drop icons back to secondaryText; 22pt keycap chips carry a subtle white-0.05 interior fill under the white-0.16 ring (physical-key look, chip corners probe ~(40,40,50)); group gaps put the row boxes 26.5pt apart (53px at 2x, round 10) and carry a CENTERED 0.5pt white-0.08 rule -- a row-mean luminance scan across the live gap (2026-08-10) found the single-row hairline at (48,49,62) that the earlier coarse threshold scan missed ("pure whitespace" was wrong); do not re-delete it BUT a 0.5pt white-0.08 hairline runs under the SEARCH FIELD of BOTH modals edge-to-edge (probed (49,50,62) over the (31,32,42) sheet; the round-4 "browse has no rule" read was a coarse-scan miss -- round 10 finds the same single-pixel rule 94px from the browse sheet top; do not re-delete either); the actions sheet HUGS its content -- the list height is computed from the fixed-height rows (`listHeight`), never a bare greedy `.frame(maxHeight:)` (a greedy ScrollView left a ~116pt empty tail) and never a height cap (a 400pt cap hid Show Sidebar/Change Theme below the fold; the live menu shows its whole ~500pt list and the overhang panel already screen-clamps); inapplicable actions set `RaycastAction.isEnabled=false` and dim to 40% with selection/commit skipped (live disabled icons probe (82,85,101)); search caret rests 20pt in, tinted primaryText (not accent blue). The actions menu carries Raycast's full first group in their order -- New Note ⌘N, Duplicate Note ⌘D, Pin/Unpin Note ⇧⌘P, Browse Notes ⌘P, Go Back ⌘[, Go Forward ⌘] -- ALL real remappable `ShortcutAction`s (the pin case is `togglePin`: the LEGACY rawValue "pinNote" is a retired one-note-era key that stored bindings still drop, so reusing it would resurrect ⌘S) with live keycap chips, dispatched through the one binding table. Duplicate copies the serialized text into a new note; Go Back/Forward walk `ChatSession`'s back/forward stacks (browser semantics: navigation pushes the departed note and clears forward; rows dim when their stack is empty). New Note and Duplicate both dim while the current note is empty, like the live menu (the new note would be an identical blank). Their glyphs are exact @raycast/icons rasters (`RaycastDuplicate/Tack/ArrowLeftCircle/ArrowRightCircle.png`, rendered white-on-transparent at 128px from the package's path data -- Pin Note uses Raycast's TACK icon, not the map-pin). Second group: Find in Note ⌘F, Copy Note ⌘C; third: Change Theme. Raycast rows with no honest SpotNote equivalent (Copy Note As..., Copy Deeplink, Create Quicklink) are deliberately absent unless David asks. The browse modal reveals pin + trash on the HOVERED row (Raycast order: pin first) as Raycast's OWN glyphs -- the Tack and `RaycastTrash` rasters at frame 19.5 (~17pt visible; the live buttons probe 34px tall at 2x -- SF Symbols at 13pt read visibly small and off-family, 2026-08-10) tinted PRIMARY text, not secondary (live icons peak at 221, the title's own brightness; the secondary tint read washed-out). Browse rows sit on a 70pt pitch (`rowPitch`; live title-top to title-top is 140px at 2x -- ours squeezed to 130) with a 7pt title-to-metadata gap (live 27px at 2x; spacing-2 rendered 14) and the LIST HUGS CONTENT to six full rows before scrolling (`listHeight`; the live modal shows six 70pt rows in a 1000px sheet -- the old 320pt cap cut row five; the full 6-row sheet CLIPS the last row's box by ~6px at 2x, so the hug subtracts 3pt at six rows, and the "Notes" header zone is 38.5pt with the cap-top 37px below the hairline, round 10). Row titles are `RaycastFont.medium(16)` (live ascender-height 25px at 2x; 17 rendered 27) and the metadata line uses the BRIGHTER `metadataText` #9EA3B9, not `secondaryText` -- live metadata probes top-decile 168 vs 152, Raycast runs two secondary tones (chips stay #8E93A9); pin rides `ChatStore.setPinned` (no `updatedAt` bump -- pinning is not an edit) and pinned notes sort first. The old embedded fuzzy palette + separate preview panel are retired. The notes sidebar is REMOVED ENTIRELY (David 2026-08-10: "remove the sidebar cmd \\ thing entirely") -- no shelf panel, no `sidebarShown` pref, no `toggleSidebar` shortcut (its rawValue joins the retired keys that stored bindings drop on load, so a stale ⌘\ binding dies quietly); do not reintroduce any of it. The panel frame is fully programmatic: the `NSHostingView` has `sizingOptions = []`. With vim mode on, `VimModePill` (bottom bar TRAILING overlay, in the retired T-button's slot: 38pt circle, 10pt from the right edge) shows the nvim-style mode badge: a circled letter that MORPHS N/I/V via `ModeGlyphShape` (three stroked lines at the icon set's stroke weight -- 1.8pt at the 18pt glyph, matching @raycast/icons' 1.5/16 ratio -- flubber-style equal-point interpolation through `AnimatableVector12`). Ring-only like the old T button (white-0.04 fill so the bar surface shows through; tint lives in the ring + letter): insert = probed caret red `#EB5545`, normal = probed Current-dot blue `#64A1F1`, visual = family violet `#9B7CF2`. Pill glyph frames inside `RaycastTopBar.iconPill`'s 30pt hover cells are intentionally UNEQUAL (⌘ raster 20pt, cards 20pt, drawn + 18pt): rasters carry internal padding, drawn shapes none -- each frame is sized so the VISIBLE glyph hits the live app's uniform ~17.5pt and visible glyph-to-glyph gaps stay at the probed 15.5pt.
- Empty/short notes open compact and grow per line, like Raycast Notes: `EditorMetrics.roomyVisibleLinesFloor == 1`, so the empty window is 670x177pt (60 top bar + 20 inset + one 41pt line + 56 bottom bar -- pixel-measured against the live Raycast empty window). The old 6-line roomy floor was based on a ref screenshot that had content.
- The HUD opens in the bottom-right corner of the visible frame, inset by `SpotlightWindowController.defaultEdgeInset` from the right and bottom edges, and is bottom-anchored so it grows upward as content reflows. Guarded by `SpotlightWindowControllerTests.defaultHUDOriginHugsRightEdge` and `defaultHUDOriginHugsBottomEdge`.
- The editor card has no top-right copy icon; keep copy available through keyboard/menu actions instead of visible chrome.

## Safe edit flow

1. Inspect state first:

   ```bash
   git status --short --branch -uall
   ./scripts/verify-source-of-truth.sh
   ```

2. Make the smallest focused change.
3. Run focused tests for touched behavior, then the normal ladder:

   ```bash
   ./scripts/fmt-check.sh
   ./scripts/lint.sh
   ./scripts/test.sh
   ./scripts/build.sh release
   ```

4. Stage exact files only; never use broad `git add -A` in a dirty tree.
5. Commit the source change before live install unless David explicitly asks for an uncommitted visual trial.
6. Install only from this repo:

   ```bash
   ./scripts/install-release.sh
   ```

`install-release.sh` refuses dirty installs by default, backs up the current app under `~/Library/Application Support/SpotNote/AppBackups/`, installs `build/SpotNote.app`, verifies codesigning, and checks the custom binary fingerprints before launching.

For agent/CI launch checks that must not steal David's active Space, use the explicit headless smoke path instead of opening the HUD:

```bash
./scripts/headless-smoke.sh release
SPOTNOTE_FINAL_LAUNCH_MODE=headless ./scripts/install-release.sh
```

Headless launch uses `SPOTNOTE_HEADLESS_TEST=1`, initializes the app bundle, verifies no visible SpotNote windows were created, then exits/cleans up. Normal user launch remains HUD-first. If `SPOTNOTE_FINAL_LAUNCH_MODE=headless` is used for final install verification, it exits by design; before handing SpotNote back to David, relaunch `/Applications/SpotNote.app` normally and read back `pgrep -fl '/Applications/SpotNote.app|SpotNote'` so the global-hotkey app is resident.

## Manual smoke after install

- Launch/toggle SpotNote with `⌘⇧Space`.
- Confirm the panel opens in the bottom-right corner (inset from the right/bottom edges) and grows upward as you type.
- Confirm editor text renders in the system sans at the Raycast body scale.
- Confirm `- ` bullets continue with Enter/normal-mode `o`, Tab indents, and Shift-Tab outdents.
- Confirm Title-Case headings; in Vim normal mode, `,d`/`,t` jump to `## Todo`/`## Tray` and drop onto a fresh `- ` bullet at the end of each (creating the section if absent). `\t` appends the current line to spotnote-tray.md; `\c` appends the current bullet to the hermes-build State.md and clears it; `\f` tidies blank-line spacing around headers.
- Confirm there is no task checkbox/sign gutter and `gg` does not shift text into old checkbox space.
- Confirm no bottom Vim statusline appears.
- In Vim normal mode, confirm `s` starts inline Flash labels, `f` limits labels to the current line, and `K` opens the transient gutter with row labels.
- Type a task, optionally with `#Label` and `due:today` / `due:tomorrow` / `due:MM-dd-yyyy`, then use Vim normal-mode `gd`/`gp`/`gt`/`gs`/`gl` for Done/Planned/Triage/Started/Later Linear handoff to the Personal workspace, or `gc` for a Triage+Build issue in the Code workspace. The bullet should delete only after successful handoff and show a Hermes toast.

## Swift tooling footguns

These bit real sessions; check them before claiming green:

- **Report CI status from the repo's own gate, not a bare tool.** `make ci` / `scripts/ci.sh` (and `scripts/periphery.sh`) pass the repo configs. A bare `periphery scan --strict` ignores `Tools/.periphery.yml` (`retain_objc_accessible`, `retain_assign_only_properties`) and reports false dead-code "failures"; a bare `swiftlint` likewise misses the config. Use the scripts.
- **swift-format vs SwiftLint `opening_brace`:** a multi-line `if`/`guard` condition makes `swift-format` put `{` on its own line, which SwiftLint's `opening_brace` rejects. Hoist the condition into a `let` so the statement is single-line.
- **`swift-format` forbids trailing commas** in collection literals (`[TrailingComma]`); don't leave one on the last element. `./scripts/fmt.sh` auto-fixes.
- **`lizard` mis-parses `await x.get(...)` (the `get` getter keyword) and inline trailing closures**, inflating a function's reported length into a false complexity failure. Fix by extracting the closure to a `let`, or annotate the function `// #lizard forgives`.
- **Optional stored field in a memberwise init:** `let x: T? = nil` is OMITTED from the synthesized memberwise init (callers can't set it); `var x: T? = nil` triggers SwiftLint `redundant_optional_initialization`. To keep it `let` *and* settable, write an explicit init (and `// #lizard forgives` if the param count trips lizard).
- **Changing tested behavior trips `verify-source-of-truth.sh`:** it hard-codes contract greps (test names, constant values, heading spellings, AGENTS.md phrases). A behavior change isn't done until you sweep those greps here too, or `make install` aborts.
- **Install from a worktree:** `install-release.sh`/`verify-source-of-truth.sh` gate on the canonical repo root; pass `SPOTNOTE_ALLOW_NONSTANDARD_ROOT=1`. Never `swift run` during dev loops — it launches a second GUI instance; use `swift build`/`swift test` and the installer.

## Boundaries

- Do not print or commit secrets from local config, notarization, Sparkle, or Hermes ingress setup.
- Do not modify `/Applications/SpotNote.app` without a backup and post-install readback.
- Do not treat the installed binary as source. If app strings and repo source diverge, stop and recover/migrate the source before building over the app.
