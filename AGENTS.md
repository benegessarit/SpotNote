# SpotNote Agent Runbook

SpotNote's stable source of truth on David's Mac is this repo:

```text
/Users/davidbeyer/Projects/SpotNote
```

Do not build or install SpotNote from disposable worktrees such as `.hermes/kanban`, `.paperclip-*`, `/tmp`, or ad hoc recovery directories. Those are allowed for experiments only; any accepted work must be copied or recreated here, git-tracked, tested, and committed before it replaces `/Applications/SpotNote.app`.

## Current custom contract

The local David build intentionally differs from upstream SpotNote:

- Catppuccin and Rosé Pine themes are present in `Sources/Spotlight/Theme.swift`.
- Linear handoff is routed through `Sources/Spotlight/ScratchpadHandoff.swift` to the local Hermes ingress endpoint.
- Hermes feedback uses `Sources/Spotlight/HermesToastView.swift` plus `Sources/Spotlight/Resources/HermesLogo.png`.
- The first/default note is the vault-backed Markdown inbox at `~/Documents/knowledge/Captures/spotnote-inbox.md`; JSON chat persistence is secondary app-local library state, not the launch buffer. Recognized section headings are **Title Case** -- `## Todo`, `## Tray` -- and the inbox loader normalizes any recognized heading (any case, any `#` level) to that canonical spelling. The loader does NOT inject or reorder sections; header-less notes stay header-less, and a fresh/missing inbox opens blank instead of inserting a default section.
- The old Vim statusline is removed entirely: no `vimBarHeight` constant and no `VimStatusLine.swift` source file should exist.
- The floating action menus ride a borderless child panel (`RaycastModalWindow.swift`) whose frame clamps to the anchor screen's `visibleFrame` bottom (minus the 40pt tap margin): on a short bottom-pinned note the sheet slides UP over the note body instead of rendering below the display edge.
- Native Flash-style Vim jumps are restored without terminal embedding or statusline chrome: `VimFlash.swift`, `MultilineEditorFlash.swift`, and `MultilineEditorFlashRendering.swift` own the `S` whole-document backward jump, `f`/`F` same-line jumps, and `K` row/gutter labels. Normal/visual `s` is the hop-style word-hint jump (`VimWordHint.swift` + `MultilineEditorWordHint.swift`): every visible word start gets an instant label (hop.nvim TrieBacktrackFilling charset/order, nearest-first by Manhattan distance with x_bias 10), typed keys narrow labels (never a search query), the document dims while hints show, and label colors are the nvim pair (`#F75057` primary -- the `hop_red` role, rose-pine love pulled 0.6 toward true red; bare love `#eb6f92` is PINK and was a diagnosed nvim regression, do not reintroduce it -- / `#9ccfd8` foam alternate for touching runs). Both word-hint and Flash labels render as BARE colored letters, baseline-aligned in the anchor's character cell (`drawHintLabel`; word hints plain, Flash bold) -- nvim's foreground-only `hl_mode = "replace"` look; the opaque pink pill chips read as "gross background ovals" (David, 2026-08-10). The covered glyphs are hidden with a `.clear` TEMPORARY foreground attribute at refresh time (layout keeps every advance, so nothing reflows); never hide them by EDITING the string -- character replacement garbled the note text in this proportional editor (David, 2026-08-09). The hidden span is advance-MEASURED (`hintHiddenRange` in `MultilineEditorFlashRendering.swift`): the helper walks composed characters from the anchor accumulating advances until the label's ink fits (min one char, stops at a newline). On today's mono grid that equals label-length cells, but keep the measured walk -- a fixed label-length hide is exactly what broke under the earlier proportional editor ("the flash hints are all messed up", David 2026-08-10: wide labels collided with the next glyph, narrow ones left stray ink), and the helper is font-agnostic.
- The vim layer runs an OPERATOR × RANGE-PRODUCER grammar (S1 of the engine program, 2026-08-10): `VimOperators.swift` owns `VimOperator` (delete/change/yank), `VimRangeTarget` (motions + text objects), `VimMotionClass` (`dj`/`dk`/`dG`/`dgg`-class motions are LINEWISE -- whole lines, like vim -- everything else charwise), and the pure `VimTextObjects` (`iw`/`aw` wave 1); `MultilineEditorVimOperator.swift` is the ONE applicator (resolve range → yank → mutate). Contracts: `cw` enters INSERT and acts as `ce` on a non-blank (the swap lives in the applicator -- the engine is pure and cannot see the text); every delete/change YANKS to the vim pasteboard (linewise entries newline-terminated -- the paste path's linewise cue); `y{motion}`/`yy` yank without mutating (caret to span start); counts MULTIPLY across the operator (`2d3w` = 6 words -- `pendingCount` × the post-operator accumulator; a single accumulator concatenated digits); `dj` on an immovable edge line ABORTS like vim; `dd`/`cc`/`cib`/`cB` keep their established behaviors. The caps/tilde family (2026-08-10, "C doesn't work"): `C` = `c$` entering insert and `Y` = `y$` (nvim's default remap, not vi's yy quirk) both ride the operator grammar; `P` pastes before (charwise at the caret, linewise above the line); `J` joins count lines with a single space eating the next line's indent; `~` toggles case and advances, clamped to the line; `X` deletes back, clamped to the line start; `r<char>` replaces count chars via a pending capture where a DIGIT is the literal replacement (`r3`), Escape aborts, and the whole command aborts when fewer than count chars remain (vim). Do not add per-pair special cases -- new operators and objects join the tables. VISUAL MODE is nvim-exact (rebuilt 2026-08-10 after David's "keeps crashing... and it's ugly" report -- his live nvim probed via headless `nvim_get_hl`): ONE engine handler drives both wises (`handleVisualMode(wise:)` -- the old twin handlers drifted); the selection paints the flat nvim Visual lift -- `CodeStyler.applyVisualSelectionColor`, a 5% (dark) / 9% (light) background→text blend that reproduces his resolved rose-pine `#221F2E`-over-`#191724`, foreground NEVER overridden, macOS accent blue never shown; a BLOCK CURSOR draws at the visual moving end (`visualBlockCursorRect` in `draw(_:)` -- NSTextView hides the native caret while a selection is active); visual `o` swaps ends, visual `p` pastes over the selection WITHOUT clobbering the register (his `"_dP` mapping), `gv` reselects the last range (any exit path captures it), `<n>G` snaps to the ABSOLUTE line via the `Motion.toLine` case (the old relative `.down(n-1)` walked from the caret), and `$`/`e` pull back one cell so the selection covers exactly vim's cells (AppKit word-forward lands PAST the word). `repairVisualCoherence` runs after EVERY action: if the engine left visual mode but the view still holds anchors (the leader-handoff class), the range is captured for gv, anchors drop, and the leftover highlight collapses -- the broken half-visual state David read as a crash cannot persist. Vim CHROME COLORS (2026-08-10, "cursor color isn't appropriate for this theme"): the normal/visual BLOCK cursor is `Theme.vimBlockCursor` -- the catppuccin ROSEWATER role his own nvim uses for every block (`theme-switch.lua apply_chrome` + Ghostty `cursor-color #f5e0dc`; mocha `#F5E0DC` dark / latte `#DC8A78` light) with the covered glyph FLIPPED to the surface color (`fillVimBlockCursor`, nvim `Cursor bg=cursor fg=base` reverse video) -- deliberately NOT `Theme.cursor` (that is the INSERT caret's Raycast-parity red) and never the heading accent, and no more 0.82-alpha wash; the visual band survives every key state because `FixedLineHeightLayoutManager.fillBackgroundRectArray` substitutes the nvim blend where AppKit would swap in the light non-key `unemphasizedSelectedTextBackgroundColor` (the gray band David flagged -- do not move this back to a view-level draw, the layout manager's fill pass is the only place that wins in all states). YANK FLASH -- "scan, lift, dissolve" (redesigned 2026-08-11, David: "think like a top animation / micro interaction designer"): a yank is an EVENT, not a state, so it does NOT wear the Visual band -- the event wears the block cursor's ROSEWATER identity (`yankGlowColor`, peak alpha 0.24). Three beats, total 430ms: (1) SCAN -- a soft-edged glow (26pt gradient lead) sweeps the span's union left→right in 130ms ease-out, painted UNDER the text in `drawBackground`, rounded 4pt when the shape allows (single line or aligned linewise block; ragged charwise spans stay square -- per-line rounding pinches at joins); (2) LIFT -- the yanked glyphs themselves (the REAL glyph run via `layoutManager.drawGlyphs`, true colors, temporary attrs included) rise 14pt and dissolve OVER the text in `draw(_:)` (`drawYankGhost`), the copy visibly leaving for the register; spans over 1500 chars skip the ghost (`ghostCharacterCap` -- a yG of a huge note would redraw the whole run every frame) and play glow-only; (3) DISSOLVE -- the glow holds to 190ms then decays on pow(1-t, 1.7), never linear. ALL timing lives in `YankFlashCurve.sample(at:)` as pure testable functions -- the drawing holds no clocks or easing; do not inline new timing into the draw code. Applies to every yank path (operator y, yy/Y, visual y both wises); edits clear it (`didChangeText`); deletes do NOT flash. TYPING HOT PATH: `CodeStyler.apply` runs on every text change, and its full-document temporary-attribute clear invalidates the whole layout -- on a note with NO backticks (after a pass that left no attributes) it now SKIPS entirely (`codeStylerLeftAttributes`); all code styling keys on backticks, so the skip is exact. BAND DEPTH (2026-08-11, "still not really visible"): the Visual/yank band blend is 11% dark / 15% light -- nvim's RELATIONSHIP (flat surface→text lift, no accent hue, foreground untouched) at David's deliberately stronger depth; the probed-nvim 5%/9% values are superseded, do not restore them for "nvim exactness". LINE MOVES (same round): `<M-j>`/`<M-k>` are his mini.move maps -- Option chords become nvim-style `<M-x>` tokens in `vimKey` (chars from `charactersIgnoringModifiers`, so the token carries the letter, not the Option glyph; unknown Option keys still fall through and type in insert mode), the shared `moveLinesAction` serves BOTH normal mode (caret line) and visual/visual-line (the whole line block covering the selection, selection + anchors riding along so repeats chain), `executeMoveLines` in `MultilineEditorVimEdit.swift` hops the adjacent line over the block with silent clamping at buffer edges and conserves text length at the document tail by donating/borrowing the newline.
- `/` FLASH-SEARCH (2026-08-11, the port of David's flash.lua `modes.search`): `VimSearch.swift` (pure core) + `MultilineEditorVimSearch.swift` (view lane) own it, and the lane is FULLY VIEW-OWNED -- it never touches the ⌘F `FindController`, so `applyFindHighlight` structurally cannot fight it and ⌘F stays byte-identical; do not route vim search through the find bar. Typing after `/` searches live (literal, case-insensitive, capped at `VimSearchCore.matchCap` 500 -- the counter wears a trailing `+`), bands paint in `drawBackground` (dim band = 7% dark / 10% light background→text blend, one step under the 11/15 Visual band; the CURRENT match wears the Visual band color), and the caret rides the current match (first at/after the ORIGIN caret, wrapping). EXTEND-BEATS-LABEL is the core invariant: a typed char that could extend the query (the lowercased next composed char after any match -- a literal-search property: every extended match starts at an existing one) always extends; labels draw only from the flash charset MINUS that extension set (`survivingKeys`), and on overflow only the `survivingKeys.count` matches NEAREST the caret get labels, the rest staying band-only -- NEVER multi-char trie labels; this is a DELIBERATE divergence from `s` word-hints' TrieBacktrackFilling, because a search prompt must keep every key typeable. Labels render BOLD flash-green (0.651, 0.890, 0.631) through the shared `drawHintLabel` pipeline, covered glyphs hidden by the same `.clear` temporary-attribute advance walk. A label char JUMPS AND COMMITS with bands OFF (his flash `jump = {nohlsearch, register, autojump}`); Enter commits with bands ON; `n`/`N` step with wrap and RE-LIGHT the bands; `*` searches the word under the caret (alphanumerics + underscore, scan-forward when between words; lands committed, bands on); `:noh` hides bands but KEEPS the query so `n` re-lights; any edit clears the whole search; Escape restores the origin caret. The prompt surface is `VimCmdline` in the bottom bar (composed by `VimAwareBottomBar`): vim's cmdline aligned to the editor's text column (`EditorMetrics.textLeadingGap`) and set in the editor's own mono face at editor size (David 2026-08-11 "align the searchbar with the main editor text... same size as the text itself") -- counter-gray bolt/chevron sigil + query in theme ink + thin caret bar + counter-gray live count, with the centered character counter fading while a prompt is open; after Enter it collapses to a bare counter-gray residue, so nothing search-flavored shows before `/`. Before this surface existed, `/` and `:` were typed BLIND. `VimController` keeps only prompt/counter plumbing (`setSearchStatus`, `searchClearHandler`) and owns NO search logic. TEST FOOTGUN: the view holds `vimController` WEAKLY -- a test that discards the controller (`let (textView, _) = ...`) deallocates it mid-test and the prompt silently dies; bind it and use it past the last keystroke.
- The editor font is **Lilex Nerd Font Mono** (`LilexNFM-Regular`/`-Bold` bundled in Resources, system-mono fallback) at the Raycast Notes body scale via `SpotNoteFont.editor()` -- the SAME face David's nvim renders in (Ghostty `font-family`; IBM Plex Mono letterforms + ligatures). David ordered the mono grid back on 2026-08-10 ("switch to mono grid and use the same mono font as my nvim") after the proportional 2026-08-08 "exact same look" era made the hop/flash letter hints gappy; only the note BODY is mono -- all Raycast chrome stays Inter via `RaycastFont`. Bold/italic derive in-family via `NSFontManager` trait conversion (guarded by `boldFlashLabelsKeepTheMonoAdvance`).
- Task checkboxes/sign markers are retired from the live editor. Old Markdown `[ ]` / `[x]` storage markers may still parse for compatibility, but SpotNote should not render, reserve, or toggle checkbox gutter chrome. Task completion/status now flows through Linear motions.
- Markdown outline behavior belongs in `MarkdownOutline.swift` and `PlaceholderTextView`: Enter and Vim normal-mode `o`/`O` continue `- ` bullets with the current indentation, Tab indents a bullet by one two-space level, Shift-Tab outdents by one level, and pressing Enter on an empty bullet exits the list. Motions are split by prefix: the `,` prefix jumps to a section and inserts a fresh `- ` bullet at its end (creating it if absent, ignoring internal blank spacers): `,d` → `## Todo`, `,t` → `## Tray`. The `g` prefix is reserved for Linear handoff: `gd/gp/gs/gt/gl` send the bullet to David's Personal workspace at Done/Planned/Started/Triage/Later, and `gc` sends it to his Code workspace at Triage with the assignable `Develop` label. The `\` leader handles other sends and is reusable: `\t` appends the current line to spotnote-tray.md; `\c` appends the current bullet block(s) to the hermes-build State.md (`~/Documents/knowledge/Work/hermes-build/Notes/State.md`) as one flattened `- ` line per block, then clears the source (`N\c` files N separate `- ` lines, not one fused line); and `\f` (or `:fmt`) tidies blank-line spacing around section headers (one blank line above and below each header, none above the top header), preserving bullets and multiline bullets. (Legacy `gH/gD/gT/tt/gy`, `,h`, `,b`, and `\h` are retired.)
- Line numbers are REMOVED (2026-08-09, David: "I don't like the line numbers") -- no preference, no Settings row, no `:set number`. The editor reserves no gutter (`LineNumberRuler.thickness(showsLineFlashHints: false, ...) == 0`) and uses `EditorMetrics.textLeadingGap == 37` for the Raycast-style body inset (pixel-measured from the live Raycast Notes window; the text edge is the parity contract). `LineNumberRuler` survives ONLY as the zero-width flash-hint gutter: it opens to `signColumnWidth` while vim `K` line-flash labels show, then collapses.
- The editor text is the Raycast Notes body scale, pixel-measured from David's live Raycast Beta Notes window (2026-08-08): `EditorMetrics.fontSize == 22` with `EditorMetrics.lineHeight == 45` (the measured 20/41 Raycast scale stepped up proportionally on David's "make the font somewhat bigger", 2026-08-10), panel width 670, on a full-bleed 26pt-radius surface (circle-fit on the live Raycast window border arc). Themes with `Theme.backgroundTop` (raycast-dark: `#2A2C3B` → `#252634`) render Raycast's full-height vertical surface gradient; flat themes keep the near-opaque glass tint. ALL panels are borderless (`SpotlightWindowController.panelStyleMask`); the traffic lights are drawn by `RaycastTrafficLights` in SwiftUI (14pt dots, 23pt centers, Display-P3 close red `#EC6765` + two disabled greys) because Raycast Notes draws its own. ALL Raycast-chrome text (top-bar title, character counter, every modal row/header/chip/search field) renders in INTER via `RaycastFont` -- the live app ships `InterVariable.woff2` in its frontend bundle and none of its chrome is SF; the bundled `Inter-Regular/Inter-Medium.otf` register through `FontLoader`, and `.custom` falls back SILENTLY on a wrong PostScript name, so never rename them casually. The top bar (60pt: hover-revealed lights, centered 16pt first-line title, trailing 44pt capsule -- solid `#25262E`, white-0.08 border -- with the exact @raycast/icons command raster, a custom fanned-cards note-switcher glyph, and a DRAWN 2pt round-cap plus -- `RaycastPlusShape`, 18pt visible; the bundled raster's stroke scales to a too-heavy 3pt at pill size, so the pill must not use it. Each pill icon sits in a uniform 30pt hover cell (`PillIconButton`) that shows a soft CIRCLE highlight under the pointer -- probed ~31-33pt diameter at a bare +8 RGB over the pill fill (white 0.04); a rounded rect at 0.08 read as the wrong shape in David's 2026-08-10 captures) and bottom bar (56pt: 16pt character counter only; theme switching lives in the actions modal, the old circled-T button is retired) follow Raycast's real resigned state (pixel-probed 2026-08-09): the pill and title STAY VISIBLE when the panel is not key -- the close light turns inactive grey like its siblings, pill icons dim to `#4C4E59` (`RaycastChromePalette.controlResigned`), and the title keeps its normal color. The traffic lights are the ONE hover-gated piece (David 2026-08-10): `RaycastTopBar.showsLights` fades the cluster in only while the pointer is over the window (or any modal is up -- a modal child window intercepts tracking events, so hover alone would drop them); the pill must NOT become hover-only. The bars are ALWAYS-present chrome: `EditorMetrics.topBarHeight`/`bottomBarHeight` are mirrored in BOTH `SpotlightRootView.extraChromeHeight` and the controller's `chromeAboveEditor`/`chromeBelowEditor`. Note browsing and actions are Raycast-style floating modals (`RaycastNotesModal` over `FuzzyController` + `RaycastActionsModal` from the ⌘ pill button in `RaycastModals.swift`; the standalone themes modal is RETIRED -- theme switching is the Change Theme flyout submenu): the editor stays UNDIMMED while a modal shows (Raycast probes (40,42,56) with the menu open, 2026-08-09 -- a transparent tap-catch in a `SpotlightRootView` `.overlay` only dismisses; do not reintroduce a dim), and the SHEET rides an overhanging borderless CHILD WINDOW (`RaycastModalWindow.swift`, presented via `.background(RaycastModalOverhang(...))`) so it extends past the panel's bottom edge exactly like the live app (probed: sheet top 100pt below window top, 383.5pt wide, the live menu runs ~427pt past the empty window's bottom). The child panel takes key for the search field; every resign decision routes through the pure `ModalFocusPolicy` over the `RaycastModalOverhang.ownedWindows` weak table (the HUD's focus-loss handler, each child panel's own resign observer, and the submenu's three-way keep/close/dismiss-all) so key moving INSIDE the window family never dims or closes anything, the chrome passes `isKey || anyModalShown` to stay lit, and the child losing key to a non-family window dismisses the modal. Modals NEVER contribute panel height (guarded by `SpotlightRootToastTests.notesModalDoesNotTriggerPanelHeightCallbacks`). Menu geometry is pixel-probed (2026-08-09): the sheet is the SYSTEM GLASS MATERIAL (round 12: Raycast's `[macos]` stylesheet puts `-apple-visual-effect: -apple-system-glass-material` on `.popover__webPopover` and paints no ink over it -- the gradient stack in the sibling rule is the non-macOS fallback -- and the live sheet transmits ~48% of backdrop per channel; `SpotNoteGlassBackdrop` renders it BARE on macOS 26+, NO tint overlay, since any overlay cuts transmission -- the round-3 swatch-lab popover+0.45 pair only reaches t~0.2 and survives strictly as the pre-26 fallback; the child panel pins `.darkAqua` and draws NO window-server shadow: its hard contact rim was a near-black 1-2px ring hugging the border that the live menu lacks, and their own CSS shadow is soft 10-20% black, invisible on the dark desk), a 0.75pt fg-20 border (the live edge profile at 2x is ONE strong pixel + one falloff; our 1pt drew two full-strength pixels and read heavy -- round 12), radius 16.5 (circle-fit 33px at 2x on the live corner arc); the search zone is 47pt in both modals (caret/placeholder center 47px at 2x -- round 10) over a 0.5pt fg-10 hairline (a SINGLE pixel at 2x; our 1pt rule read twice as heavy); row highlights inset 9.5pt -- 728px wide inside the 766px sheet at 2x, content pads compensated so icon/title ink stays put -- and actions rows are 43pt TOUCHING, the live icon centers riding an 86.0px pitch at 2x (five consecutive deltas, round 12; the round-10 "84px" was the highlight rect read short) (radius 6, selected fill = TRANSLUCENT selection-5: their `--list-item-hover-background-color: var(--selection-5)` is 5% of the fg over the sheet, which reproduces the probed (40,40,52)/(38,38,50) composites exactly -- the old opaque #272733 matched the probes too but blocked the glass bleed inside the highlight), and the highlight FOLLOWS THE POINTER in both modals (hover lights a row without a click, staying on the last hovered row -- keyboard and pointer write the same selection); row icons are exact @raycast/icons rasters, frames tuned per raster padding so the VISIBLE glyph hits the live menu's ~17pt (34px of ink at 2x on every ratio-0.875 row, round-12 same-frame capture -- the earlier 17.5pt frames drew 31px and read small) -- ratio-0.875 rasters get frame 19.5, `RaycastTextSearch` 20, the plus also 19.5 (its cross ink is intrinsically smaller: live 24px vs the siblings' 34); cards glyph at scale 0.845; Find in Note uses `RaycastTextSearch` -- Raycast's lines+magnifier glyph is NOT in the public icon set, so its SVG sits in Resources beside the raster, composed from David's capture geometry) tinted in the PRIMARY text color #CFD6F1 -- the live rows read as one bright unit, do not drop icons back to secondaryText; 22pt keycap chips are HOLLOW like the live ones -- transparent interior under a 1pt fg-20 ring, glyph fg-60, radius 4.5, 2.5pt gap (Raycast's own stylesheet: `[data-variant=default]` = transparent bg + 1px inset `--color-border-token` ring; the earlier white-0.05 "physical key" fill was the visible difference David flagged); group gaps put the row boxes 26.5pt apart (53px at 2x, round 10) and carry a CENTERED 0.5pt fg-10 rule -- a row-mean luminance scan across the live gap (2026-08-10) found the single-row hairline at (48,49,62) that the earlier coarse threshold scan missed ("pure whitespace" was wrong); do not re-delete it BUT a 0.5pt fg-10 hairline runs under the SEARCH FIELD of BOTH modals edge-to-edge (probed (49,50,62) over the (31,32,42) sheet; the round-4 "browse has no rule" read was a coarse-scan miss -- round 10 finds the same single-pixel rule 94px from the browse sheet top; do not re-delete either); the actions sheet HUGS its content -- the list height is computed from the fixed-height rows (`listHeight`), never a bare greedy `.frame(maxHeight:)` (a greedy ScrollView left a ~116pt empty tail) and never a height cap (a 400pt cap hid Show Sidebar/Change Theme below the fold; the live menu shows its whole ~500pt list and the overhang panel already screen-clamps); inapplicable actions set `SpotNoteCommand.isEnabled=false` and dim to 30% -- their `[data-disabled]{opacity:.3}`; 0.3 x fg + 0.7 x sheet composites to (82,87,105) against the live probe (82,85,101), which the old 0.4 (fg-only division) misread -- with selection/commit skipped; search caret rests 20pt in, tinted primaryText (not accent blue). The actions menu carries Raycast's full first group in their order -- New Note ⌘N, Duplicate Note ⌘D, Pin/Unpin Note ⇧⌘P, Browse Notes ⌘P, Go Back ⌘[, Go Forward ⌘] -- ALL real remappable `ShortcutAction`s (the pin case is `togglePin`: the LEGACY rawValue "pinNote" is a retired one-note-era key that stored bindings still drop, so reusing it would resurrect ⌘S) with live keycap chips, dispatched through the one binding table. Duplicate copies the serialized text into a new note; Go Back/Forward walk `ChatSession`'s back/forward stacks (browser semantics: navigation pushes the departed note and clears forward; rows dim when their stack is empty). New Note and Duplicate both dim while the current note is empty, like the live menu (the new note would be an identical blank). Their glyphs are exact @raycast/icons rasters (`RaycastDuplicate/Tack/ArrowLeftCircle/ArrowRightCircle.png`, rendered white-on-transparent at 128px from the package's path data -- Pin Note uses Raycast's TACK icon, not the map-pin). Second group: Find in Note ⌘F, Copy Note ⌘C; third: Change Theme (a flyout submenu row, not a perform -- see the submenu bullet below). Raycast rows with no honest SpotNote equivalent (Copy Note As..., Copy Deeplink, Create Quicklink) are deliberately absent unless David asks. The browse modal reveals pin + trash on the SELECTED row (their shipped notes-window JS renders accessories under `e.isSelected`; hover still works because pointer and keyboard write the same selection) in Raycast order (pin first) on a 20pt gap with 6pt extra trailing (round 12: the live glyph-ink gap is 48px at 2x and the trash ink rests 51px from the sheet edge -- 17pt/no-trailing drew 42/39) as Raycast's OWN glyphs -- the Tack and `RaycastTrash` rasters at frame 19.5 (~17pt visible; the live buttons probe 34px tall at 2x -- SF Symbols at 13pt read visibly small and off-family, 2026-08-10) tinted PRIMARY text, not secondary (live icons peak at 221, the title's own brightness; the secondary tint read washed-out). Browse rows sit on a 70pt pitch (`rowPitch`; live title-top to title-top is 140px at 2x -- ours squeezed to 130) with a 7pt title-to-metadata gap (live 27px at 2x; spacing-2 rendered 14) and the LIST HUGS CONTENT to six full rows before scrolling (`listHeight`; the live modal shows six 70pt rows in a 1000px sheet -- the old 320pt cap cut row five; the full 6-row sheet CLIPS the last row's box by ~6px at 2x, so the hug subtracts 3pt at six rows, and the "Notes" header zone is 38.5pt with the cap-top 37px below the hairline, round 10). Row titles are `RaycastFont.medium(16)` (live ascender-height 25px at 2x; 17 rendered 27) and the metadata line uses the BRIGHTER `metadataText` #9EA3B9, not `secondaryText` -- live metadata probes top-decile 168 vs 152, Raycast runs two secondary tones (chips stay #8E93A9); pin rides `ChatStore.setPinned` (no `updatedAt` bump -- pinning is not an edit) and pinned notes sort first. The old embedded fuzzy palette + separate preview panel are retired. The notes sidebar is REMOVED ENTIRELY (David 2026-08-10: "remove the sidebar cmd \\ thing entirely") -- no shelf panel, no `sidebarShown` pref, no `toggleSidebar` shortcut (its rawValue joins the retired keys that stored bindings drop on load, so a stale ⌘\ binding dies quietly); do not reintroduce any of it. The panel frame is fully programmatic: the `NSHostingView` has `sizingOptions = []`. With vim mode on, `VimModeBadge` (bottom bar TRAILING overlay, the Raycast T-button slot: 38pt, 9pt from the right edge) shows the nvim mode letter with Raycast's own circled-T treatment (David 2026-08-11 "make the n/i thing in the places where raycast puts the T in the circle" -- the earlier keycap chip + stroke-morph glyph was REJECTED as off-family; do not resurrect it): a 38pt circle filled `pillFill` with the icon pill's white-0.08 hairline ring, the letter set in `RaycastFont.medium(17)`; tint stays the only mode signal: insert = probed caret red `#EB5545`, normal = probed Current-dot blue `#64A1F1`, visual = family violet `#9B7CF2`, dropping to `controlResigned` when the panel is not key. Pill glyph frames inside `RaycastTopBar.iconPill`'s 30pt hover cells are intentionally UNEQUAL (⌘ raster 20pt, cards 20pt, drawn + 18pt): rasters carry internal padding, drawn shapes none -- each frame is sized so the VISIBLE glyph hits the live app's uniform ~17.5pt and visible glyph-to-glyph gaps stay at the probed 15.5pt.
- COMMANDS ARE DATA + the flyout SUBMENU (2026-08-11, pixel-derived from David's Raycast AI "Change Model…" capture cross-checked against Raycast's shipped `popover-DXQ3FJok.css`): `SpotNoteCommand` (SpotNoteCommand.swift) is the one registry row shape -- id/title/icon/keys/section/isEnabled/trailing/chip/submenu/perform, mirroring their shipped submenuModel -- and `RaycastActionsModal` renders FROM it; new commands are data, not view code. A command with a non-nil `submenu` provider is a PARENT row: Enter or → (empty query only) opens `RaycastSubmenu` in a SECOND stacked child panel (`RaycastSubmenuWindow.swift`) anchored to the invoking row, its dimmed parent-title header (fg-60, cap 18pt below the sheet top) riding the row's center like the live flyout expands in place. Submenu metrics live in `RaycastSubmenuMetrics`, all capture-probed at 2x: sheet 419pt wide (838px), same 43pt/86px row pitch and 9.5pt highlight inset as the actions menu, LARGE item size (icon frame 22 inset 16pt, gap 12 -- title ink lands the probed x=100px), one 20.5pt text inset shared by header/section captions/accessories, accessory ink 20pt off the right edge, section breaks 48.75pt carrying a FADED 0.5pt fg-10 rule (gradient-fill ends at 10%/90% -- `RaycastSubmenuFadeRule`; the fill IS the gradient because a `.mask` on a half-point layer rasterizes to nothing) 10.5pt under the last row; the Beta chip is the capture's rendered pill (26.5x10pt, (245,227,181) bg, near-black text, 22.5pt gap to the accessory -- the capture beats the stylesheet's yellow-10 variant guess), and the search field sits at the BOTTOM (their upwardsLayout: 53.5pt zone under a hairline inset 21pt -- NOT edge-to-edge like the top-search modals) with an optional ⓘ accessory. Keyboard: ↑/↓ wrap skipping disabled, Enter commits, Esc and ← (empty query) return to the parent menu; typing filters sections live, EMPTIED SECTIONS DROP WITH THEIR HEADERS; keyboard selection fills selection-10 (their data-selected -- stronger than hover), pointer hover fills selection-5 separately. The search-zone height is derived, not probed (the capture crops at the sheet bottom) -- re-probe when David captures a full-height flyout. First consumer: Change Theme (themes grouped Dark/Light, swatch dots, "Current" trailing on the active theme, selected on open; `SpotNoteCommands.themeSubmenu`); `RaycastThemesModal` is deleted -- do not resurrect a standalone theme picker. RESIGN CONTRACT: `ModalFocusPolicy` (RaycastModalWindow.swift) is the single pure decision table for every key-loss reaction across the window family (`RaycastModalOverhang.ownedWindows`); both dismissal layers (HUD focus-loss + per-panel resign observers) and the submenu's keep/close-submenu/dismiss-all all consult it, decisions deferred one runloop tick because the key transfer may still be in flight when `didResignKey` fires. ModalFocusPolicyTests drives it with real windows, no key loop.
- Empty/short notes open compact and grow per line, like Raycast Notes: `EditorMetrics.roomyVisibleLinesFloor == 1`, so the empty window is 670x181pt (60 top bar + 20 inset + one 45pt line + 56 bottom bar; the 22/45 body is David's deliberate step up from the measured Raycast 20/41). The old 6-line roomy floor was based on a ref screenshot that had content.
- MEASUREMENT GROUND TRUTH for Raycast parity (round 11, 2026-08-10): Raycast Notes' UI is web-rendered from stylesheets SHIPPED ON THIS MAC -- `/Applications/Raycast Beta.app/Contents/Resources/macos-app_RaycastDesktopApp.bundle/Contents/Resources/frontend/*.css` (+ the notes-window JS beside it). Read the CSS before pixel-probing captures: it gives exact structure (hollow kbd chips, `--shadow-panel-border: inset 0 0 0 1px var(--fg-20)` = the menu border, accessories gaps, selected-row accessory visibility). CAVEAT: CSS lengths are in `--spx` = zoom-scaled px and the notes window renders at David's zoom, so rendered sizes still come from capture probes -- use CSS for relationships/colors, captures for absolute pt. The modal ink system is ONE foreground `#CFD6F1` (`RaycastModalPalette.primaryText`) at opacity tiers: 100% titles/icons, 60% secondary + chip glyphs (`secondaryInk`), 20% menu border + chip rings (`borderInk`), 10% hairlines (`hairlineInk`) -- all four verified against independent pixel probes; derive new grays from the tiers, never hand-pick. SCROLL: the browse list is an EAGER `VStack` (LazyVStack hitched mid-flick), keyboard moves animate `scrollTo` (0.16s easeOut) while hover-driven selection NEVER re-scrolls (rows sliding under the pointer retargeted selection and fought the trackpad -- the rubber-band jank), the modal child window is SHADOWLESS (round 12 -- no invalidateShadow churn exists at all), and the editor's TextKit-1 layout manager opts into `allowsNonContiguousLayout` (viewport layout; without it long notes re-lay out fully per edit).
- The HUD opens in the bottom-right corner of the visible frame, inset by `SpotlightWindowController.defaultEdgeInset` from the right and bottom edges, and is bottom-anchored so it grows upward as content reflows. Guarded by `SpotlightWindowControllerTests.defaultHUDOriginHugsRightEdge` and `defaultHUDOriginHugsBottomEdge`.
- The editor card has no top-right copy icon; keep copy available through keyboard/menu actions instead of visible chrome.

## Safe edit flow

1. Inspect state first:

   ```bash
   git status --short --branch -uall
   ./scripts/verify-source-of-truth.sh
   ```

2. Make the smallest focused change.
3. Run focused tests for touched behavior, then the normal ladder:

   ```bash
   ./scripts/fmt-check.sh
   ./scripts/lint.sh
   ./scripts/test.sh
   ./scripts/build.sh release
   ```

4. Stage exact files only; never use broad `git add -A` in a dirty tree.
5. Commit the source change before live install unless David explicitly asks for an uncommitted visual trial.
6. Install only from this repo:

   ```bash
   ./scripts/install-release.sh
   ```

`install-release.sh` refuses dirty installs by default, backs up the current app under `~/Library/Application Support/SpotNote/AppBackups/`, installs `build/SpotNote.app`, verifies codesigning, and checks the custom binary fingerprints before launching.

For agent/CI launch checks that must not steal David's active Space, use the explicit headless smoke path instead of opening the HUD:

```bash
./scripts/headless-smoke.sh release
SPOTNOTE_FINAL_LAUNCH_MODE=headless ./scripts/install-release.sh
```

Headless launch uses `SPOTNOTE_HEADLESS_TEST=1`, initializes the app bundle, verifies no visible SpotNote windows were created, then exits/cleans up. Normal user launch remains HUD-first. If `SPOTNOTE_FINAL_LAUNCH_MODE=headless` is used for final install verification, it exits by design; before handing SpotNote back to David, relaunch `/Applications/SpotNote.app` normally and read back `pgrep -fl '/Applications/SpotNote.app|SpotNote'` so the global-hotkey app is resident.

## Manual smoke after install

- Launch/toggle SpotNote with `⌘⇧Space`.
- Confirm the panel opens in the bottom-right corner (inset from the right/bottom edges) and grows upward as you type.
- Confirm editor text renders in the system sans at the Raycast body scale.
- Confirm `- ` bullets continue with Enter/normal-mode `o`, Tab indents, and Shift-Tab outdents.
- Confirm Title-Case headings; in Vim normal mode, `,d`/`,t` jump to `## Todo`/`## Tray` and drop onto a fresh `- ` bullet at the end of each (creating the section if absent). `\t` appends the current line to spotnote-tray.md; `\c` appends the current bullet to the hermes-build State.md and clears it; `\f` tidies blank-line spacing around headers.
- Confirm there is no task checkbox/sign gutter and `gg` does not shift text into old checkbox space.
- Confirm no bottom Vim statusline appears.
- In Vim normal mode, confirm `s` starts inline Flash labels, `f` limits labels to the current line, and `K` opens the transient gutter with row labels.
- In Vim normal mode, confirm `/` opens the bolt capsule with live match bands and green labels, a label char jumps immediately (bands off), Enter + `n`/`N` cycle with the counter, `*` searches the word under the caret, and ⌘F still opens the stock find bar unchanged.
- Open the ⌘ actions menu, arrow to Change Theme, press → or Enter: the flyout submenu opens anchored on the row (dimmed "Change Theme…" header, Dark/Light sections, swatch dots, "Current" on the active theme, search at the BOTTOM). Arrows move, typing filters, Enter applies the theme, Esc/← walks back to the actions menu without closing it, and clicking the note dismisses the whole stack without closing the HUD.
- Type a task, optionally with `#Label` and `due:today` / `due:tomorrow` / `due:MM-dd-yyyy`, then use Vim normal-mode `gd`/`gp`/`gt`/`gs`/`gl` for Done/Planned/Triage/Started/Later Linear handoff to the Personal workspace, or `gc` for a Triage+Build issue in the Code workspace. The bullet should delete only after successful handoff and show a Hermes toast.

## Swift tooling footguns

These bit real sessions; check them before claiming green:

- **Report CI status from the repo's own gate, not a bare tool.** `make ci` / `scripts/ci.sh` (and `scripts/periphery.sh`) pass the repo configs. A bare `periphery scan --strict` ignores `Tools/.periphery.yml` (`retain_objc_accessible`, `retain_assign_only_properties`) and reports false dead-code "failures"; a bare `swiftlint` likewise misses the config. Use the scripts.
- **swift-format vs SwiftLint `opening_brace`:** a multi-line `if`/`guard` condition makes `swift-format` put `{` on its own line, which SwiftLint's `opening_brace` rejects. Hoist the condition into a `let` so the statement is single-line.
- **`swift-format` forbids trailing commas** in collection literals (`[TrailingComma]`); don't leave one on the last element. `./scripts/fmt.sh` auto-fixes.
- **`lizard` mis-parses `await x.get(...)` (the `get` getter keyword) and inline trailing closures**, inflating a function's reported length into a false complexity failure. Fix by extracting the closure to a `let`, or annotate the function `// #lizard forgives`.
- **Optional stored field in a memberwise init:** `let x: T? = nil` is OMITTED from the synthesized memberwise init (callers can't set it); `var x: T? = nil` triggers SwiftLint `redundant_optional_initialization`. To keep it `let` *and* settable, write an explicit init (and `// #lizard forgives` if the param count trips lizard).
- **Changing tested behavior trips `verify-source-of-truth.sh`:** it hard-codes contract greps (test names, constant values, heading spellings, AGENTS.md phrases). A behavior change isn't done until you sweep those greps here too, or `make install` aborts.
- **Install from a worktree:** `install-release.sh`/`verify-source-of-truth.sh` gate on the canonical repo root; pass `SPOTNOTE_ALLOW_NONSTANDARD_ROOT=1`. Never `swift run` during dev loops — it launches a second GUI instance; use `swift build`/`swift test` and the installer.

## Boundaries

- Do not print or commit secrets from local config, notarization, Sparkle, or Hermes ingress setup.
- Do not modify `/Applications/SpotNote.app` without a backup and post-install readback.
- Do not treat the installed binary as source. If app strings and repo source diverge, stop and recover/migrate the source before building over the app.
